/**
 * Data-proxy, gets data from the api, but if is 1st time fetch it gets the existing value
 * (unless the force flag is applied)
 */

import api from "../api/mocks/mockApi"

const _results // we store the results here

export default {
  
}
